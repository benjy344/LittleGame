<div class="loading in">
  <div class="screen glitch js-glitch">
<!--     <div class="glitchContent is-off"><span class="text js-text"></span></div>
 -->    <div class="figure"></div>
    <div class="figure-mask"></div>
  </div>  
</div>
<h1>LittleGame</h1>


<% if user_signed_in? %>

<% if current_user.avatar.nil? %>
	<p>
		<%= link_to 'créer mon avatar', new_avatar_path, class: 'btn' %>
	</p>
<% else %>
	<!-- <p>
		<%# link_to 'mon avatar', current_user.avatar, class: 'btn' %>
	</p> -->
	<p>
		<%= link_to 'Donjons', donjons_path, class: 'btn' %>
	</p>
	<% if current_user.avatar.countUnlockDonjon > 1 %>
	<p>
		<%= link_to 'Le Village', town_path, class: 'btn' %>
	</p>
	<% end %>
<% end %>


<% else %>
	<p>
		<%= link_to 'Commencer', new_user_session_path, class: 'btn' %>
	</p>
<% end %>